const initializeStore = () => {
  // called when the store is initialized
  // called after every mutation.
  // The mutation comes in the format of `{ type, payload }`.
  // console.log('Initialize Store', store)
}

export default initializeStore
